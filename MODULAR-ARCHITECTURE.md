# Modular Architecture

RoomMate Pro is split into small feature files while keeping the MERN stack unchanged.

Frontend: React + Vite
Backend: Node.js + Express
Database: MongoDB + Mongoose
Realtime: Socket.IO
Authentication: JWT + bcrypt

See `READABLE-CODE-GUIDE.md` for the file-by-file map.
