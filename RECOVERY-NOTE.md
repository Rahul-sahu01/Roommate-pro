# Recovery note

The later versions introduced large authentication/payment/AI/security changes. This archive intentionally does not include those changes. It preserves the v4 application source as the rollback baseline.

If you already have MongoDB data, the project files do not contain the database itself. Your data remains in MongoDB and is not deleted by replacing the project folder.
